import React from 'react';
import classnames from 'classnames';

export interface SearchProps extends IProps {}

const Search: React.FC<SearchProps> = (props) => {
    const { children, className, style } = props;
    return <div></div>;
};

export default Search;
